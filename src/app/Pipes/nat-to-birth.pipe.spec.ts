import { NatToBirthPipe } from './nat-to-birth.pipe';

describe('NatToBirthPipe', () => {
  it('create an instance', () => {
    const pipe = new NatToBirthPipe();
    expect(pipe).toBeTruthy();
  });
});
