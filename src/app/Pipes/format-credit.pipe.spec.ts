import { FormatCreditPipe } from './format-credit.pipe';

describe('FormatCreditPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCreditPipe();
    expect(pipe).toBeTruthy();
  });
});
