import { ShadowIncreaseDirective } from './shadow-increase.directive';

describe('ShadowIncreaseDirective', () => {
  it('should create an instance', () => {
    const directive = new ShadowIncreaseDirective();
    expect(directive).toBeTruthy();
  });
});
